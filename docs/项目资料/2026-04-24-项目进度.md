# 项目进度

更新时间：2026-04-24

## 当前状态

- 已移除历史打卡业务、题库、排行、订单、优惠券、奖金等旧业务代码。
- 已保留基础系统能力、小程序壳子、微信登录入口、用户积分与 VIP 字段、微信支付底层入口。
- 菜单初始化与 seed 脚本已调整为基础系统版本，并补充旧业务菜单清理脚本。
- `docs/` 已整理为中文分类目录结构，当前项目资料统一收敛到 `docs/项目资料/`。
- 项目英文名已统一为 `language-learning` / `Language Learning`。

## 当前结论

- 当前仓库适合作为“语言学习”项目的二次开发基线。
- 新业务接入前，建议先完成数据库初始化并重新跑一轮基础 seed。

## 下一步动作

- 配置 `.env` 与数据库连接。
- 执行 `prisma/seed.ts`、`scripts/init-menus.ts`、`scripts/init-roles.ts`。
- 在 Web 与小程序端开始接入新的语言学习业务模块。

## 阻塞项

- 如果本地或目标环境未配置 `DATABASE_URL`，数据库清理与初始化脚本无法实际落库执行。
